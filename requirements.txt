# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# --- Config ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Database / ORM ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3   # PostgreSQL driver (Supabase) — set DATABASE_URL=postgresql+psycopg://…

# --- Scheduling ---
apscheduler==3.11.0

# --- Scraping ---
playwright==1.49.1
beautifulsoup4==4.12.3
lxml==5.3.0
httpx==0.28.1

# --- Matching ---
rapidfuzz==3.11.0

# --- LLM: relevance/sentiment scoring + e-paper page reading (Claude vision) ---
anthropic==0.42.0

# --- Groq: e-paper vision + YouTube Whisper transcription ---
groq==0.13.1

# --- Timezones (ZoneInfo on Windows) ---
tzdata==2024.2

# --- YouTube bulletin audio (watch URL → local file for Groq Whisper) ---
yt-dlp>=2025.01.01

# --- Screenshot footer overlay + e-paper image handling ---
pillow==11.1.0
opencv-python-headless==4.10.0.84
